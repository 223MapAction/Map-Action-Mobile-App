import { Dimensions } from "react-native";

export const EndPoint = "http://mapaction.withvolkeno.com/api";
export const DIMENSIONS = {
  width: Dimensions.get("screen").width,
  height: Dimensions.get("screen").height,
};

export const COLORS = {
  app: "#2d9cdb",
};
